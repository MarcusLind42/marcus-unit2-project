  <form action="/pokemon/<%= profile._id %>/createteam" method="POST">
    <button type="submit">Create Team</button>
  </form>


  function createTeam(req, res) {
  PokemonTeam.create({trainer: req.user.profile._id})
    .then(pokemonTeam => {
      pokemonTeam.save()
        .then(function() {
          res.redirect('/pokemon', {
            profile: profile
          })
        })
    })
}